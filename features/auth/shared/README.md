# FeatureAuthShared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build FeatureAuthShared` to build the library.

## Running unit tests

Run `nx test FeatureAuthShared` to execute the unit tests via [Jest](https://jestjs.io).
