# DomainUserShared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build DomainUserShared` to build the library.

## Running unit tests

Run `nx test DomainUserShared` to execute the unit tests via [Jest](https://jestjs.io).
